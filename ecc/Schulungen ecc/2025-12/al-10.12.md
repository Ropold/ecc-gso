
Processing only report

im alten nav - Secctions sind layouts
layout existiert nicht mehr in BC

für jedes dataitem werden sections automatisch hinzufügt
Toolbox => neue felder

connection-string fornav:
bc cloud & onPrem Universal code (AL-extension)

ZUGFeRD steht für "Zentraler User Guide des Forums elektronische Rechnung Deutschland"
und ist ein Standard für elektronische Rechnungen, der in Deutschland entwickelt wurde.

xrechnung von fornav funktioniert nur, wenn es ein fornav breicht ist

var currentdate: Date;
currentdate := WorkDate();
currentdate := DYMY2Date(Format(Today(),0,'<Day>.<Month>.<Year>'));

xml-port 