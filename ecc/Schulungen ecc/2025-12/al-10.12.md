
Processing only report

im alten nav - Secctions sind layouts
layout existiert nicht mehr in BC

für jedes dataitem werden sections automatisch hinzufügt
Toolbox => neue felder

connection-string fornav:
bc cloud & onPrem Universal code (AL-extension)

ZUGFeRD steht für "Zentraler User Guide des Forums elektronische Rechnung Deutschland"
und ist ein Standard für elektronische Rechnungen, der in Deutschland entwickelt wurde.

xrechnung von fornav funktioniert nur, wenn es ein fornav breicht ist

var currentdate: Date;
currentdate := WorkDate();
currentdate := DMY2Date(Format(Today(),0,'<Day>.<Month>.<Year>'));


gdt where used, um zu sehen, wo gdt verwendet wird
microsoft docs

SalesHeader = Kopf des Verkaufsdokuments (Master),
Es ist die Kopfzeile eines Verkaufsdokuments (z.B. Auftrag, Angebot, Rechnung).
Enthält allgemeine Informationen, die für das gesamte Dokument gelten, z. B.:
Kundendaten (Name, Adresse, Kundennummer)
Datum (Auftragsdatum, Lieferdatum)
Zahlungs- und Lieferbedingungen
Währung, Sprache, Status

SalesLine = die einzelnen Positionen (Details).
Die zugehörigen Positionen (Artikel, Mengen, Preise) stehen in der Tabelle SalesLine,
die über das SalesHeader-Dokument referenziert wird.

publisher, eventsubscriber

dimension in al? jede dimension id ist ein auf einem datensatz (rec)?
nab extension tool,

system datatype
evaluate - Varinteger = 01089. the return code is: yes.

verbose, details mode beim publishen

urlaubsantrag als ihk würde reichen